[workspace]
[package]
name = "cowd-interactive-tests"
version = "0.1.0"
edition = "2021"
description = "Interactive TUI+API test framework using tmux and curl"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
