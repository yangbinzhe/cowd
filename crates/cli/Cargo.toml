[package]
name = "cli"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
auth-broker = { path = "../auth-broker" }
cowd-app-protocol = { path = "../app-protocol" }
gateway = { path = "../gateway", default-features = false }
sandbox-launcher = { path = "../sandbox-launcher" }
runtime = { path = "../runtime" }
skill = { path = "../skill/service" }
tools = { path = "../tools" }
serde.workspace = true
serde_json.workspace = true
reqwest.workspace = true
thiserror = "2"
tui = { path = "../tui", optional = true }

[[bin]]
name = "cowd"
path = "src/main.rs"

[features]
default = ["app-mfg"]
app-mfg = ["gateway/app-mfg"]
tui-surface = ["dep:tui"]
full = ["tui-surface", "app-mfg", "tui/code-highlight", "gateway/code-index"]

[dev-dependencies]
cowd-app-sdk = { path = "../app-sdk" }
serde_json.workspace = true
tempfile = "3"
tokio = { version = "1", features = ["rt-multi-thread"] }

[lints.rust]
unsafe_code = "forbid"
