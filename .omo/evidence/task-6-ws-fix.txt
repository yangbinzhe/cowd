# Task 6: WS Handler enter_runtime Fix -- Evidence

## Fixes applied to: crates/cowd-cli/src/server/mod.rs

### 1. WebSocket handler (original line 2301, now 2301-2312)
**Before**: `tokio::runtime::Handle::current().block_on(runtime.run_turn_async(...))`
**After**: `std::thread::spawn(move || { ... let rt = Builder::new_current_thread().build(); let result = rt.block_on(runtime.run_turn_async(...)); let _ = tx.send(result); })` + `rx.recv()`

### 2. Memory on_turn_end in send_message_handler (original line 3306, now 3306-3310)
**Before**: `let rt = tokio::runtime::Handle::current(); rt.block_on(async { ... })`
**After**: `let rt = tokio::runtime::Builder::new_current_thread().enable_all().build().expect("memory rt"); rt.block_on(async { ... })`

### 3. server_execute_turn (original line 3334, now 3337-3343)
**Before**: `tokio::runtime::Handle::current().block_on(runtime.run_turn_async(input, &prompter))`
**After**: `let turn_rt = Builder::new_current_thread().enable_all().build().expect("server_execute_turn rt"); turn_rt.block_on(runtime.run_turn_async(input, &prompter))`

## Verification
- `cargo build --release 2>&1 | grep -c '^error'` → 0
- `grep -n 'Handle::current()' crates/cowd-cli/src/server/mod.rs | grep -v 'try_current'` → empty (零残留)
- `Handle::try_current()` at line 1178 preserved (allowed exception)
