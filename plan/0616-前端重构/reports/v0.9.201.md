# v0.9.201 汇总报告

状态：通过

完成内容：

- 新增 WebUI app shell。
- 新增左侧竖向 nav rail。
- 新增 Graphite-like 主题基线。
- 保留旧 right panel 兼容。
- 新增 shell DOM 单元测试。
- 新增 shell Playwright 视觉测试。
- 输出 desktop/mobile 截图。

测试：

- `cd webui && npm test`：通过，80 tests。
- `cd webui && npm run test:e2e -- webui-shell.e2e.spec.js`：通过，1 test。

截图：

- `plan/0616-前端重构/screenshots/v0.9.201-desktop.png`
- `plan/0616-前端重构/screenshots/v0.9.201-mobile.png`

修复的视觉问题：

- 文件树实体文本错误。
- 旧版本号显示错误。

下一版本：

- v0.9.202 左侧 icon rail 与完整页面路由。

