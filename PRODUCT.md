# Product

## Register

product

## Users

Cowd 面向在终端、WebUI 与其他 Edge Surface 中完成代码阅读、工具执行、长任务协作和运行诊断的开发者与操作者。用户需要在同一会话中看清系统正在做什么、为什么这样做，并能以键盘为主持续输入、纠偏、取消和查看证据。

## Product Purpose

Cowd 是由统一 Gateway 与 Runtime 驱动的 AI 执行系统。它把模型推理、工具调度、会话持久化、审批、证据和多个交互 Surface 组织成一套可恢复、可观察、可审计的工作流，而不是各前端各自维护一份运行真相。

## Brand Personality

- 简洁：默认界面只呈现当前任务所需的信息。
- 友好：状态、错误与下一步都使用清楚、克制、可行动的语言。
- 沉着：长任务和故障期间持续给出可靠反馈，不用夸张动画制造忙碌感。
- 强大：支持高吞吐、并行和高级能力，但通过渐进披露避免把复杂度倾倒给用户。
- 诚实：明确区分已接受、执行中、等待、完成和失败，不伪造进度。

## Anti-references

- 不冒充 Claude、Codex 或模型供应商；供应商名称仅是运行元数据，助手身份始终是 Cowd。
- 不让用户在发送后面对无状态、无耗时、无取消入口的黑箱等待。
- 不把 TUI 做成常驻仪表盘；主路径保持紧凑，详情按需展开。
- 不为视觉换行修改用户输入原文，也不因终端缩放改变提示词语义。
- 不让 TUI、WebUI、Gateway 各自推导一套互相冲突的执行状态。
- 不用不受控并发冒充加速；并行必须受纯度、依赖、资源冲突和预算约束。

## Design Principles

1. 任务优先：输入、当前状态、输出和下一动作构成首要视觉层级。
2. 因果可见：用户能区分提交、排队、模型调用、思考、工具、审批、收尾和终态。
3. 单一事实源：Runtime 生成执行事实，Gateway 传输，Surface 只做投影和短暂传输态。
4. 键盘可预期：编辑、换行、历史、取消、排队和焦点行为稳定，不抢焦点。
5. 有界性能：自动利用安全并行，同时保持顺序、取消、背压和资源上限。
6. 身份是契约：基础身份稳定、不可被来源提示或项目说明替换，来源只能追加指导。
7. 证据优先：验收以实际请求、事件、快照、性能数据和删除证明为准。

## Accessibility & Inclusion

- 支持纯键盘操作，并为取消、展开详情和队列操作提供可发现的快捷提示。
- 正确处理中文、宽字符、emoji、组合字符、粘贴文本、窄终端和 tmux 缩放。
- 状态不能只依赖颜色表达，同时提供图标或文字；颜色遵循终端主题并保证对比度。
- 尊重 reduced motion；动画不是理解状态的必要条件。
- 错误和警告使用明确语言，不将内部协议名直接暴露为用户任务。
