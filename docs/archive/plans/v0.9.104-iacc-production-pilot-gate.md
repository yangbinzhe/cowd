# v0.9.104 IACC Production Pilot Gate

## Goal

Close the v0.9.100-v0.9.104 lift by adding a single production pilot gate that validates the complete IACC chain:

```text
source pack ingest
-> metric dependency / incremental compute
-> attention / evidence / quality gate
-> incident room
-> digital employee skill plan
-> action execution
-> feedback
-> memory case promotion
-> playbook recommendation
-> cockpit report delivery visibility
-> WebUI module tests
```

## Implemented Capabilities

- Added health capability `production_pilot_gate`.
- Added `scripts/v09104_iacc_production_pilot_gate.sh`.
- The gate runs:
  - `scripts/v09100_iacc_memory_case_playbook_scenario.sh`
  - `scripts/v09101_iacc_digital_employee_skill_pack_scenario.sh`
  - `scripts/v09102_iacc_command_center_incident_room_scenario.sh`
  - `scripts/v09103_iacc_source_pack_large_data_scenario.sh`
  - `scripts/v0998_iacc_production_release_gate.sh`

## Acceptance

Run:

```bash
IACC_WEBUI_NODE_MODULES=/media/yi/Ext/cowd-webui-deps/node_modules \
IACC_V0998_SKIP_SCALE_BENCHMARK=1 \
scripts/v09104_iacc_production_pilot_gate.sh
```

The command must exit `0`.

