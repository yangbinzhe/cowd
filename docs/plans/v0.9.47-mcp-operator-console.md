# v0.9.47 MCP Operator Console

## Goal

Expose MCP as an operator-managed connector plane without starting or blocking on live MCP discovery from the WebUI control path.

## Scope

- Add `/api/connectors/mcp/servers` for configured server readiness.
- Render WebUI `MCP Operators` with server counts, status, transport, missing required fields, and diagnostics.
- Keep MCP capabilities in the existing connector contract for TUI/WebUI alignment.

## Acceptance

- `CARGO_TARGET_DIR=/tmp/cowd-v0947-target cargo test -p cowd-cli connector_routes_project_configured_mcp_servers_into_runtime_contract --no-default-features`
- `npm test -- modules.test.js`
- `cargo fmt --check && git diff --check`
