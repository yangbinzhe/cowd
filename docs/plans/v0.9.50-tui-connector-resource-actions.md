# v0.9.50 TUI Connector Resource Actions

## Goal

Make connector resource lifecycle and memory promotion usable from TUI through the existing command palette and daemon projection API.

## Scope

- Add TUI actions for connector resource revalidation and memory promotion.
- Add projection client methods for `/api/connectors/resources/revalidate` and `/api/connectors/resources/promote-memory`.
- Add command palette dynamic actions for the first projected connector resource.
- Keep GatewayPanel as the operational view and command palette as the action dispatch path.

## Acceptance

- `CARGO_TARGET_DIR=/tmp/cowd-v0950-target cargo test -p cowd-cli projection_client --no-default-features`
- `CARGO_TARGET_DIR=/tmp/cowd-v0950-target cargo test -p cowd-cli sync_runtime_actions_adds_connector_entries --no-default-features`
- `CARGO_TARGET_DIR=/tmp/cowd-v0950-target cargo test -p cowd-cli local_connector_resource_state_updates_projection_state --no-default-features`
- `cargo fmt --check && git diff --check`
