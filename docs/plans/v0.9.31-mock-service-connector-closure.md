# v0.9.31 Mock ServiceConnector Closure

## Version Goal

Prove the Service Plane without calling real external SaaS APIs.

## Scope

- Add service connector contracts in runtime.
- Add `MockDocsServiceConnector`.
- Expose mock docs tools and execute endpoint.
- Convert service execution into `CrossPlaneAction`.
- Record policy decision and execution receipt.
- Return `ExternalResourceRef` in the service result.

## Non-goals

- No persistent ResourceDirectory yet.
- No real Feishu connector.
- No write operations.

## TDD Plan

- Runtime mock connector returns service capabilities and canonical resource ref.
- API lists mock docs tools.
- API execute returns service result, receipt, policy decision, and resource ref.
- Idempotency replay returns previous receipt.

## Acceptance

- `cargo test -p runtime connector`
- `cargo test -p cowd-cli mock_docs`
