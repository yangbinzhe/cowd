# v0.8.12 Memory Kernel Refactor

## Design Correction

The memory architecture must stay pure.

The target is not a stack of RAG, vector search, graph databases, wiki pages, snapshots, and agent notes.

The target is one coherent memory organism:

```text
Experience -> Trace -> Pattern -> Orientation -> Action -> Revision
```

Five memory layers remain valuable, but they are a governance structure. Trace / Pattern / Orientation is the runtime organization model.

This means:

- L0-L4 define authority, scope, and write rules.
- Trace/Pattern/Orientation defines how knowledge moves through the organism.
- Session and agent integration must go through one MemoryKernel, not direct ad-hoc calls.

## Core Principle

Cowd memory should not become smarter by keeping more active content.

It should become smarter by maintaining the shortest reliable path from current intent to valid orientation.

Large-scale memory is achieved by:

- preserving traces.
- organizing patterns.
- keeping orientation small and authoritative.
- making all cold knowledge reachable through paths.
- never deleting history just because it is no longer active.

## Second-Order Architecture Audit

Verdict:

- the current architecture is directionally correct.
- it is pure enough to avoid becoming a RAG/vector/wiki mixture.
- it can support the final vision only if the dynamic self-organization layer is made explicit.

The previous design describes the memory substrate well:

- Atom.
- Evidence.
- Link.
- State.
- Recall.

But a living memory system also needs a pulse. That pulse must not become a new storage concept. It is a repeated control loop over the same five concepts.

### Philosophical Test

Memory is not storage.

Memory is the continuity mechanism between:

- what happened.
- what is currently believed.
- what should guide action now.
- what must remain recoverable when today's belief changes.

The correct product goal is not "remember everything in the prompt". It is:

```text
Preserve the whole past, expose the current truth, and keep the path between them explainable.
```

This resolves the apparent contradiction between forgetting and omniscience:

- forgetting means "not active now".
- archival means "not foregrounded".
- supersession means "not authoritative now".
- deletion means "only when explicitly requested or legally required".

### Scientific Test

The design aligns with useful cognitive distinctions without copying biology:

- Trace is episodic evidence.
- Pattern is semantic/procedural structure.
- Orientation is working memory plus policy-relevant belief.
- State transition is consolidation and reconsolidation.
- Recall is cue-driven path activation.
- Salience decay is attention management, not data loss.
- Conflict handling is belief revision.

The system should not train its own neural network as the core mechanism. That would reduce transparency and increase operational risk. Self-evolution should happen through inspectable changes to Atom, Link, State, summaries, clusters, and orientation rules.

### Practical Test

The design is practical if the fast path and slow path are separated:

Fast path:

- accept evidence immediately.
- index exact text and minimal metadata immediately.
- return degraded but usable memory packets if enrichment is unavailable.
- never block a user turn on distillation, embedding, graph maintenance, or import.

Slow path:

- extract atoms.
- attach links.
- detect conflicts.
- consolidate repeated traces into patterns.
- update salience and state.
- rebuild affected orientation caches.

This keeps the system alive under load instead of turning memory into a blocking pipeline.

### Purity Test

The architecture stays pure only if all advanced capabilities are expressed as operations over the same primitives:

- RAG is candidate generation over Evidence and Atom.
- graph is Link traversal.
- wiki is a rendered view of Pattern and Orientation.
- session summary is an Atom derived from Evidence.
- embedding is an optional index over Evidence/Atom text.
- memory palace is a scope/navigation view, not a new store.
- multi-agent knowledge is L4 atoms and links, not a separate memory system.

Any feature that cannot be represented by Atom, Evidence, Link, State, or Recall should be rejected or redesigned.

### Consistency Test

The five layers and the runtime organization are not competing hierarchies:

```text
L0-L4 = authority, scope, visibility, mutation rights.
Trace/Pattern/Orientation = information state.
Lifecycle events = movement between states.
MemoryKernel = enforcement point.
```

This means every item can be described by both axes:

- L2 Trace: a test failure log.
- L2 Pattern: "this repo uses X architecture".
- L2 Orientation: "for this task, follow X architecture".
- L4 Trace: a sub-agent transcript.
- L4 Pattern: "two agents agree this module is risky".
- L4 Orientation: "shared warning for this task".

## Living Memory Pulse

Every memory cycle should be expressible as:

```text
Sense -> Encode -> Associate -> Judge -> Consolidate -> Orient -> Act -> Reflect -> Reorganize
```

This is the organic loop. It maps to the implementation lifecycle:

- Sense: capture session/tool/document/agent evidence.
- Encode: create candidate atoms from evidence.
- Associate: create links to scope, entity, symbol, session, agent, task, and decision.
- Judge: apply authority, confidence, conflict, recency, repetition, and verification.
- Consolidate: promote stable repeated candidates into patterns.
- Orient: select small active guidance for the current turn.
- Act: inject the packet into session/agent execution.
- Reflect: record what was used, omitted, contradicted, or corrected.
- Reorganize: update salience, state, clusters, links, and caches.

No step is allowed to mutate protected memory silently. Reorganization is not free-form rewriting; it is auditable state transition.

### Self-Organization Mechanisms

Self-organization must be explicit and bounded:

- Assimilation.
  New evidence is accepted quickly, fingerprinted, indexed, scoped, and queued for enrichment.

- Differentiation.
  Mixed memories are split into smaller claim-like atoms when they contain multiple decisions, facts, preferences, or tasks.

- Association.
  Atoms are linked to nearby entities, symbols, sessions, files, agents, tasks, and prior atoms.

- Consolidation.
  Repeated or high-confidence atoms become patterns; stable high-authority patterns may become orientation.

- Reconsolidation.
  When old memory is recalled in a new context, it can be re-judged against new evidence and either reinforced, narrowed, contradicted, or superseded.

- Homeostasis.
  Active orientation is kept small; salience decays; stale atoms leave the foreground; hot conflicts are surfaced; background jobs are throttled.

- Reflection.
  The system produces memory health reports and per-layer summaries that a user or agent can inspect.

### Evolution Boundary

The memory may evolve:

- links.
- state.
- confidence.
- salience.
- validity windows.
- summaries.
- clusters.
- orientation rules within approved policy.

The memory may not silently evolve:

- L0 protected doctrine.
- explicit user preferences against later inference.
- verified project facts without newer verification.
- private agent/session scope into shared scope.
- evidence content.

This gives Cowd an evolving mind without an ungoverned mutable identity.

### Memory Homeostasis

The system needs a homeostatic regulator. It should run as background maintenance and as cheap checks during prepare:

- layer budget pressure.
- orientation token pressure.
- conflict pressure.
- orphan evidence count.
- atom without evidence count.
- broken link count.
- stale active atom count.
- repeated failed recall paths.
- slow query/index warning.
- per-agent shared-memory leak checks.

Homeostasis outputs actions:

- demote to stale.
- mark as candidate.
- request confirmation.
- schedule distillation.
- rebuild links.
- rebuild FTS/vector index.
- split atom.
- merge/supersede duplicates.
- emit degraded status.

Homeostasis is the difference between a memory database and a living memory system.

### Layer Visibility

Each layer must be explicitly presentable.

L0 Constitution view:

- active doctrine.
- protected entries.
- pending proposals.
- version history.
- last mutation authority.

L1 Core Instinct view:

- active preferences.
- inferred habits.
- user corrections.
- confidence/reinforcement.
- superseded older habits.

L2 Project Mind view:

- current architecture orientation.
- project decisions.
- symbol-linked memories.
- test/code evidence.
- stale project assumptions.

L3 Deep Memory view:

- topic clusters.
- archived evidence.
- historical timelines.
- recalled cold paths.
- distillation queue.

L4 Shared Collective view:

- shared agent knowledge.
- consensus records.
- conflicts.
- private/shared boundaries.
- handoff lineage.

These views should not be separate stores. They are projections of Atom/Evidence/Link/State/Recall filtered by layer.

### Transparency Contract

Every active orientation item must answer:

- What layer owns this?
- Is it Trace, Pattern, or Orientation?
- What evidence supports it?
- What authority activated it?
- What older memory did it supersede?
- What conflicts exist?
- Why was it selected for this turn?
- Why was it allowed into this agent/session scope?

If the system cannot answer these questions, the memory is not mature enough to guide enterprise-grade agent behavior.

## Five-Layer Governance Model

### L0: Constitution

Purpose:

- identity, system doctrine, protected constraints, human-defined or AI-established foundational rules.

Authority:

- user/system highest authority.
- AI may propose L0 changes, but cannot silently mutate protected L0.

Runtime role:

- Orientation.

Implementation:

- existing `MemoryLayer::L0`.
- write source limited by `MemoryWriteGuard`.
- explicit audit required for mutation.

Best mechanisms:

- deterministic storage.
- explicit versioning.
- no vector-only recall.
- direct pinning into context when enabled.

### L1: Core Instinct

Purpose:

- stable working preferences, default behavior, durable habits, model/session operating assumptions.

Authority:

- user explicit > repeated confirmed behavior > AI inference.

Runtime role:

- Orientation plus high-confidence Pattern.

Implementation:

- existing `MemoryLayer::L1`.
- short TTL cache is valid, but entries must still carry source and state.

Best mechanisms:

- preference extraction.
- reinforcement scoring.
- bounded active cache.
- user correction supersession.

### L2: Project Mind

Purpose:

- project conventions, architecture, code symbols, tests, current requirements, project-specific decisions.

Authority:

- repository facts and tests > project decisions > tool observations > assistant inference.

Runtime role:

- Pattern and Orientation.

Implementation:

- existing `MemoryLayer::L2`.
- `ProjectScopeManager`, code indexer, symbol-memory links, project KG.

Best mechanisms:

- FTS for exact symbols/errors.
- code symbol graph.
- project KG.
- incremental file fingerprinting.
- decision lineage.

### L3: Deep Memory

Purpose:

- long-term knowledge, document insights, compressed sessions, historical observations, low-frequency but reusable knowledge.

Authority:

- evidence-backed active claims can be promoted; stale or superseded claims remain historical.

Runtime role:

- Trace and Pattern.

Implementation:

- existing `MemoryLayer::L3`.
- verbatim evidence and compressed summaries.

Best mechanisms:

- hybrid FTS/vector retrieval.
- temporal state.
- cluster distillation.
- cold archive with path recall.

### L4: Shared Collective

Purpose:

- team memory, cross-agent decisions, delegation results, peer observations, shared project context.

Authority:

- consensus and role reputation matter, but cannot override L0 or verified project facts.

Runtime role:

- Pattern and shared Orientation.

Implementation:

- existing `MemoryLayer::L4`.
- `SharedLayer`, L4 event bus, agent directory, reputation, memory sync.

Best mechanisms:

- scope/visibility fence.
- event-driven peer updates.
- consensus/conflict records.
- parent_session_id and task lineage.

## Runtime Organization Model

### Trace

Raw or near-raw experience:

- session messages.
- tool outputs.
- documents.
- file observations.
- agent transcripts.

Trace is high volume and mostly cold. It is preserved for evidence and reconstruction.

### Pattern

Structured meaning extracted from traces:

- decisions.
- preferences.
- entity facts.
- recurring errors.
- code relationships.
- document topic clusters.

Pattern is the navigational fabric of memory.

### Orientation

Current effective knowledge:

- active L0/L1.
- current project doctrine.
- accepted architecture.
- active task state.
- relevant team decisions.

Orientation is small, authoritative, and directly injected into session/agent context.

## Minimal Core Data Model

Do not introduce many conceptual stores. Normalize around five concepts.

### Atom

The one interpreted memory unit.

Maps to current `MemoryEntry`.

Required fields:

- id.
- layer.
- scope.
- source.
- content/title.
- confidence.
- salience.
- state.
- validity.
- source_agent.
- session_id.
- evidence pointer.

### Evidence

Immutable source material.

Maps to current `VerbatimSink`, blob store, session events, tool sandbox, and imported documents.

### Link

Typed relationship.

Unifies:

- `MemoryEntry.relations`.
- KG triples.
- symbol-memory links.
- session links.
- agent links.
- supersedes/contradicts/supports.

### State

Temporal lifecycle state:

- candidate.
- active.
- conflicted.
- superseded.
- stale.
- archived.

### Recall

Audit record:

- query/intent.
- selected atoms.
- omitted atoms.
- reasons.
- evidence pointers.
- conflicts.
- budget.

## Lifecycle

Every session/agent turn must pass through the same loop:

```text
Bind -> Prepare -> Observe -> Relate -> Judge -> Distill -> Activate/Archive -> Explain
```

### Bind

Bind:

- session_id.
- project_id.
- agent_id.
- team_id.
- task_id.
- model/context budget.

### Prepare

Build a `MemoryContextPacket`:

- orientation.
- supporting patterns.
- warnings.
- conflicts.
- evidence pointers.
- dropped/omitted reasons.

### Observe

Capture session messages, tool outputs, and agent results as trace/evidence.

### Relate

Attach links:

- entity.
- symbol.
- session.
- agent.
- project.
- decision lineage.
- contradiction/supersession.

### Judge

Apply:

- authority.
- confidence.
- recency.
- repetition.
- scope.
- user correction.
- code/test verification.

### Distill

Convert repeated traces into patterns and stable patterns into orientation.

### Activate/Archive

Keep current orientation bounded. Archive does not delete.

### Explain

Every injected memory must be able to answer:

- why this?
- why now?
- from what evidence?
- why not the older version?

## Current Code Reality

### Strong Existing Assets

- `crates/memory/src/types.rs`
  - `MemoryEntry`, `MemoryLayer`, `MemoryScope`, `AgentVisibility`, `Relation`, `HandoffData`.

- `crates/memory/src/orchestrator.rs`
  - five-layer routing.
  - active scope/session/agent.
  - L4 team recall.
  - fact checking and conflict downgrade.
  - closet routing.

- `crates/memory/src/cognitive.rs`
  - facade for prepare_context, extraction, maintenance, handoff, symbol links, audit, performance.

- `crates/memory/src/store/sqlite.rs`
  - SQLite, FTS5, verbatim, entities/triples, symbols, symbol references.

- `crates/memory/src/write_guard.rs`
  - write source to layer policy.

- `crates/runtime/src/conversation.rs`
  - memory manager exists in runtime.
  - prepare_context is called before request assembly.
  - memory callback and handoff exist.

- `crates/runtime/src/agent.rs`
  - sub-agent memory injection.
  - sub-agent result shared to L4.

- `crates/runtime/src/joint_problem_solving.rs`
  - pipeline result persisted to L4.

- `crates/cowd-cli/src/api_routes.rs`
  - memory status, layers, entries, search, entities, triples, symbol links.

- `crates/cowd-cli/src/tui/components/memory_panel.rs`
  - real cognitive manager-backed memory panel.

- `crates/cowd-cli/src/tui/components/l4_knowledge_view.rs`
  - real L4/team memory view.

### Critical Integration Gaps

1. No MemoryKernel boundary.
   Runtime, sub-agents, JPS, API, and TUI call `CognitiveContextManager` directly.

2. Session/project binding is incomplete.
   Runtime sets `active_agent=primary` but does not uniformly bind session/project/task before every memory lifecycle operation.

3. L0/L1 governance is not yet productized.
   WriteGuard exists, but protected mutation, audit, proposal, and user approval paths are not unified.

4. Relations are fragmented.
   `relations_json`, KG triples, symbol links, and session/agent links are not one path API.

5. State is implicit.
   Confidence/staleness exist, but candidate/active/superseded/conflicted/archived is not a first-class lifecycle.

6. Recall is still mostly retrieval.
   It should become path-based orientation selection with explanation.

7. Large-scale memory needs cold reachability.
   `VerbatimSink`, ToolSandbox, vector, FTS, and KG exist, but there is no single cold-to-active promotion policy.

## Reference Systems Review

This section is based on local inspection of sibling `agents` implementations. The goal is not to copy their product metaphors. The goal is to extract their strongest mechanisms and fit them into Cowd's five-layer governance plus Trace/Pattern/Orientation runtime model.

### MemPalace

Useful mechanisms:

- scope-first organization.
  MemPalace's wing/room/hall/tunnel structure is valuable because it makes retrieval predictable before ranking begins. For Cowd, this maps to `MemoryScope`, `ProjectScopeManager`, agent scope, session scope, and task lineage.

- bounded wake-up.
  Its L0/L1 wake-up model keeps always-loaded context small, then loads L2/L3 only when a topic appears. Cowd should preserve this discipline: L0/L1/L2 orientation must be bounded, and L3/L4 should enter through recall paths.

- closet-like pointer layer.
  The closet layer is useful as a compact topic/entity pointer to evidence, not as a separate user-facing store. Cowd should implement this as Pattern indexes that point to Evidence, never as another memory concept.

- graph traversal from shared rooms.
  MemPalace uses shared room names across wings as tunnels. Cowd should generalize this with typed `MemoryLink` path recall across project, session, agent, entity, symbol, and decision links.

- hybrid candidate union.
  Its searcher treats vector retrieval, BM25, and closet hits as candidate/ranking signals. Cowd should use the same principle: vector is one candidate source, FTS and links are mandatory candidate sources.

- temporal knowledge graph.
  The entity triple design with validity windows matches Cowd's need for "forgotten does not mean deleted". Current facts should be active; superseded facts remain historically queryable.

- fail-soft large corpus handling.
  The graph builder explicitly skips bad metadata rather than failing a whole query. Cowd memory paths must follow this rule: one corrupted atom/evidence/link cannot break a session.

Rejected or constrained:

- Chroma cannot become Cowd's required core dependency.
  Cowd already has SQLite/FTS and optional vector configuration. External vector backends remain optional accelerators.

- the palace metaphor should not leak into Cowd's core API.
  Cowd should expose atoms, evidence, links, state, and recall packets, not drawers/closets/wings as first-class product nouns.

- automatic import of legacy local files is not allowed.
  Consistent with the session decision, local memory/session files can be passively imported by user action, not silently consumed at startup.

### opencode

Useful mechanisms:

- event-sourced session core.
  opencode defines structured session events and projects them into queryable messages, usage, agent/model state, tool calls, and session metadata. Cowd should use the same principle for memory: raw Trace events remain immutable, and current memory views are projected from them.

- projector boundary.
  Its `SessionProjector` is the clean separation between event stream and database state. Cowd needs an equivalent memory projector: Observe writes Trace, projector updates Atom/Link/State/Recall views.

- aggregate-scoped sequencing.
  Events are sequenced per aggregate. Cowd should sequence memory lifecycle events by session_id/project_id/agent_id so concurrent agents cannot corrupt lifecycle state.

- service interfaces over raw storage.
  Session list/get/messages/context operations sit behind a service boundary. Cowd's API/TUI/runtime should call `MemoryKernel`, not `CognitiveContextManager` or store internals directly.

- state editor pattern.
  opencode's state module serializes committed transforms and rebuilds views deterministically. Cowd can use the same concept for active orientation: committed memory state transitions produce current orientation, not ad-hoc mutation.

Rejected or constrained:

- Cowd does not need to clone opencode's full Effect/Drizzle architecture.
  The transferable idea is event -> projector -> service, not the TypeScript stack.

- memory should not become a generic session event dump.
  Session events are Evidence/Trace. Memory begins when interpretation creates Atom/Link/State.

### Hermes

Useful mechanisms:

- task-local session context.
  Hermes replaced process-global environment session state with `contextvars` to avoid concurrent message contamination. Cowd needs an equivalent invariant: active session/agent/project/task must be passed explicitly through `MemoryTurnContext`, never stored as ambient mutable global state.

- structured stream events.
  Hermes separates "what happened" from platform rendering. Cowd should separate memory lifecycle events from TUI/WebUI presentation. UI consumes packets and traces; it does not decide memory meaning.

- memory and heap monitoring.
  Hermes logs memory baselines, high-water marks, and shutdown snapshots. Cowd should add memory subsystem telemetry: prepare latency, recall candidate counts, packet token size, degraded reason, store errors, and background distillation cost.

- runtime footer/status.
  Hermes exposes compact model/context/cwd status. Cowd should expose compact memory status: active layer budget, recall confidence, degraded state, conflict count, and source scope.

- TUI store isolation.
  Hermes TUI uses small stores and gateway event handlers instead of letting UI components own runtime state. Cowd TUI memory views should subscribe to memory packet/state events rather than calling deep memory internals.

- session lifecycle cleanup.
  Hermes resets visible history, writes active-session hints best-effort, and prunes caches without blocking session creation. Cowd should use best-effort memory cleanup and never let cache/telemetry failure block session flow.

Rejected or constrained:

- Hermes' gateway/platform breadth is not the target for Cowd memory.
  Cowd should adopt concurrency and observability practices, not expand memory into a platform adapter layer.

- UI monitoring is not memory governance.
  Status panels explain memory state; they do not decide authority or activation.

## Integrated Design Amendments

### Five Layers As Control Plane

The user's five-layer framework remains the control plane:

- L0 defines non-negotiable identity, doctrine, and protected constraints.
- L1 defines stable habits, preferences, and operating instincts.
- L2 defines project truth, code facts, architecture, and current decisions.
- L3 preserves deep historical/document/session memory.
- L4 shares team/agent knowledge under visibility and authority rules.

Trace/Pattern/Orientation is the runtime data plane:

- Trace records what happened.
- Pattern organizes what it probably means.
- Orientation selects what is currently effective.

The correct synthesis is:

```text
Layer = who may write, see, override, and activate.
Trace/Pattern/Orientation = how information flows.
MemoryKernel = the only runtime boundary that applies both.
```

### Best Mechanisms Per Layer

L0 Constitution:

- storage: deterministic SQLite row plus version history.
- activation: always explicit and bounded.
- mutation: proposal -> audit -> approval -> activate.
- recall: direct by key, never vector-only.
- tests: protected writes rejected unless source is user/system-approved.

L1 Core Instinct:

- storage: atoms with evidence, confidence, reinforcement count, and supersession.
- extraction: user corrections, repeated behavior, explicit preferences.
- activation: small orientation packet with conflict warnings.
- recall: direct scope + FTS + recency/reinforcement scoring.
- tests: user correction supersedes inferred habit; stale habit remains historical.

L2 Project Mind:

- storage: project atoms, symbol links, decision lineage, test/code evidence pointers.
- extraction: file fingerprints, code symbol graph, test failures, architecture decisions.
- activation: current project orientation and active task facts.
- recall: FTS for exact symbols/errors, path recall through symbol/entity/session links, optional embedding for prose docs.
- tests: verified code/test evidence beats stale assistant inference.

L3 Deep Memory:

- storage: immutable evidence plus compressed atoms and cluster patterns.
- extraction: background distillation from sessions, documents, and tool outputs.
- activation: only via path recall and explicit relevance.
- recall: candidate union from FTS, links, vector, and temporal filters.
- tests: large cold corpus remains queryable while context packet stays bounded.

L4 Shared Collective:

- storage: agent/team atoms with visibility, reputation, parent_session_id, task lineage.
- extraction: sub-agent outcomes, joint problem solving results, handoffs, peer consensus.
- activation: shared orientation only when scope permits.
- recall: agent/task/session links plus conflict/consensus records.
- tests: private peer memory never leaks; consensus cannot override L0 or verified L2.

### Memory Lifecycle Events

Introduce explicit lifecycle events, projected into current memory state:

- `memory.trace.recorded`
- `memory.atom.proposed`
- `memory.atom.activated`
- `memory.atom.superseded`
- `memory.atom.archived`
- `memory.link.created`
- `memory.conflict.detected`
- `memory.orientation.prepared`
- `memory.recall.explained`

These events should be aggregate-scoped by at least `session_id`, and when available also by `project_id`, `agent_id`, and `task_id`.

### Passive Import Only

For memory as well as sessions:

- no JSONL as canonical storage.
- no startup auto-import.
- if local legacy files exist, show a passive import notice.
- import writes Evidence first, then runs normal Observe/Relate/Judge projection.
- failed imports create a degraded report, not a runtime crash.

### Observability Requirements

Every prepare/recall/distill operation records:

- latency.
- layer budget.
- candidate counts by source: FTS, link, vector, recent, pinned.
- selected orientation count and token estimate.
- degraded reason if any backend failed.
- conflict/supersession count.
- evidence pointer count.

This is required for enterprise-grade reliability. Without it, large-scale memory failures become invisible.

### Memory Health Metrics

Memory health is a first-class product signal. The system should expose:

- orientation pressure: active token budget used per layer.
- evidence coverage: atoms with valid evidence pointers.
- link coverage: atoms with at least one useful typed link.
- conflict pressure: active/conflicted atom ratio.
- stale pressure: active atoms past validity or confidence threshold.
- recall quality: selected vs omitted candidates and reason distribution.
- cold reachability: cold evidence found through link/FTS/vector paths.
- background lag: distillation/index/relink queue depth and oldest pending age.
- degradation state: failed backend/index/job and fallback used.
- scope safety: denied cross-agent/team visibility attempts.

These metrics make the memory visible as an organism, not a black box.

## Target Refactor Boundary

Introduce:

```rust
MemoryKernel
MemoryTurnContext
MemoryContextPacket
MemoryAtomView
MemoryState
MemoryLink
RecallTrace
MemoryHealth
MemoryDegradation
```

The kernel wraps existing `CognitiveContextManager` first. It should not rewrite everything.

Initial direction:

```text
ConversationRuntime
SubAgentRuntime
JointProblemSolving
API/TUI
        |
        v
MemoryKernel
        |
        v
CognitiveContextManager + MemoryOrchestrator + Store
```

## Best Implementation Choices

### Exact And Structured Recall

Use:

- SQLite FTS5.
- symbol index.
- scope filters.
- temporal state filters.

Reason:

- exact identifiers, errors, code names, and decisions must not depend on embeddings.

### Semantic Recall

Use:

- external embedding configuration already present in `VectorConfig`.
- vector search only as candidate generation.
- re-rank with FTS, scope, authority, recency, and relation distance.

Reason:

- embeddings are useful but not authoritative.

### Path Recall

Use:

- existing KG/symbol links/relations first.
- add a unified link table/interface.
- bounded BFS or Dijkstra-like scoring by relation type, distance, salience, and authority.

Reason:

- memory recall is path finding, not chunk similarity.

### Distillation

Use:

- deterministic heuristics for first pass.
- optional configured LLM summarizer for cluster naming and summary.
- preserve evidence pointer.

Reason:

- model capability helps interpretation, but source of truth must stay inspectable.

### Conflict And Authority

Use:

- extend existing `FactChecker` and `ConflictResolver`.
- add general claim keys beyond toy triples.
- authority scoring:
  - L0/user explicit.
  - code/test/tool verified.
  - project decision.
  - multi-agent consensus.
  - assistant inference.
  - compressed/imported low-confidence.

Reason:

- current active knowledge must be clean; old knowledge remains recoverable.

### Large Scale

Use:

- hot orientation cache.
- warm FTS/vector/link indexes.
- cold evidence/verbatim/blob.
- background distillation.
- bounded prepare_context budget.

Reason:

- all information can be findable without all information being active.

## Phase Plan

The critical path is:

```text
MemoryKernel -> State/Links -> Orientation Packet -> Governance -> Distillation -> Product Surfaces
```

Parallel work is allowed only when it does not bypass this path.

- UI work can begin after `MemoryContextPacket` has a stable read model.
- telemetry can begin with Phase 1 and expand through all phases.
- import notices can begin after Evidence write paths are stable.
- large-scale benchmarks can be scaffolded early, but pass/fail targets become meaningful after Phase 4.

### Phase 0: Living Memory Invariants

Goal:

- lock the philosophical and architectural constraints before implementation expands.
- prevent the system from degenerating into static retrieval, vector-only RAG, or ungoverned self-mutation.

Work:

- add architecture tests for the five primitive model: Atom, Evidence, Link, State, Recall.
- add packet fixtures showing Trace/Pattern/Orientation across L0-L4.
- add memory health metric structs and degraded-state enums.
- define non-blocking background job contract for distillation/relink/index repair.
- define layer visibility read models before TUI/WebUI implementation.

TDD:

```bash
cargo test -p cowd-memory memory_primitives_cover_all_memory_views
cargo test -p cowd-memory orientation_requires_evidence_or_explicit_authority
cargo test -p cowd-memory living_memory_pulse_preserves_evidence_immutability
cargo test -p cowd-memory homeostasis_never_deletes_without_explicit_policy
cargo test -p cowd-memory layer_visibility_projection_is_read_only
```

Acceptance:

- every new memory surface maps back to Atom/Evidence/Link/State/Recall.
- every active orientation item has either evidence or explicit protected authority.
- background evolution can change state/link/salience/summary, not immutable evidence.
- layer views are projections, not new stores.

### Phase 1: MemoryKernel Facade

Goal:

- create a single lifecycle boundary without breaking existing behavior.
- remove ambient mutable session/agent assumptions from memory entry points.

Work:

- add `MemoryKernel` wrapper in memory crate or runtime adapter module.
- add `MemoryTurnContext`.
- adapt runtime pre-turn prepare through kernel.
- adapt post-turn maintenance through kernel.
- keep `CognitiveContextManager` as backend.
- bind session_id, project_id, agent_id, team_id, task_id per operation.
- return structured degraded results when memory backend fails.
- add basic telemetry for prepare/post_turn latency and degraded reason.

TDD:

```bash
cargo test -p cowd-memory memory_kernel_binds_session_agent_scope
cargo test -p runtime memory_kernel_prepare_degrades_without_panicking
cargo test -p runtime memory_kernel_post_turn_preserves_turn_success
cargo test -p runtime concurrent_agents_do_not_share_memory_turn_context
```

Acceptance:

- every kernel prepare/write path has session_id and agent_id.
- memory failure cannot abort a turn.
- no new runtime call path invokes `CognitiveContextManager::prepare_context` directly.

### Phase 2: Atom State Model

Goal:

- make memory lifecycle explicit.
- make forgetting, supersession, and archival queryable without deleting history.

Work:

- introduce `MemoryState`.
- project existing `MemoryEntry` into `MemoryAtomView`.
- persist state transition records.
- default existing entries to active/candidate according to layer/source.
- add lifecycle event records for proposed/activated/superseded/archived.
- add fail-soft migration for rows missing optional metadata.

TDD:

```bash
cargo test -p cowd-memory memory_atom_projection_preserves_existing_entry
cargo test -p cowd-memory superseded_atom_is_hidden_from_active_recall
cargo test -p cowd-memory archived_atom_remains_reconstructable
cargo test -p cowd-memory bad_atom_metadata_does_not_abort_projection
```

Acceptance:

- superseded memory is not used in current orientation.
- history remains queryable.
- one malformed atom cannot break a recall.

### Phase 3: Unified Links

Goal:

- make all memory reachable through one relation model.
- turn cold memory recall into bounded path finding.

Work:

- add `MemoryLink` view over relations/KG/symbol/session/agent links.
- add path query API.
- start with existing SQLite tables where possible.
- add relation type weights for supports, contradicts, supersedes, mentions, derived_from, belongs_to, produced_by.
- add bounded traversal with max hops, source filters, and authority scoring.

TDD:

```bash
cargo test -p cowd-memory memory_links_unify_symbol_entity_session_agent
cargo test -p cowd-memory path_recall_finds_symbol_linked_decision
cargo test -p cowd-memory path_recall_respects_scope_and_visibility
cargo test -p cowd-memory path_recall_caps_expansion_on_dense_graph
```

Acceptance:

- entity/symbol/session/agent recall use one interface.
- traversal is bounded and cannot explode on dense projects.

### Phase 4: Orientation Packet

Goal:

- stop injecting loose memory chunks.
- make every injected memory explainable.

Work:

- add `MemoryContextPacket`.
- classify entries as orientation/supporting/warning/conflict.
- include omitted reasons and evidence pointers.
- convert packet to current `PreparedContext` for compatibility.
- add packet token budget and per-layer budget.
- add recall trace with selected and omitted candidates.

TDD:

```bash
cargo test -p cowd-memory memory_context_packet_prefers_orientation
cargo test -p runtime memory_packet_injected_into_system_prompt
cargo test -p cowd-cli memory_recall_explain_returns_packet_reasons
cargo test -p cowd-memory memory_context_packet_stays_bounded_on_large_candidate_set
```

Acceptance:

- active context is small, scoped, authoritative, explainable.
- UI/API can show why a memory was selected and why another was omitted.

### Phase 5: Five-Layer Governance Enforcement

Goal:

- make L0-L4 real control layers.
- prevent shared or inferred memory from overriding higher-authority truth.

Work:

- L0 protected mutation path.
- L1 user correction supersession.
- L2 code/test verified authority.
- L3 cold archive promotion.
- L4 shared agent consensus/conflict.
- add passive import notice path for legacy memory/session evidence.
- add governance audit records for denied writes and downgraded activations.

TDD:

```bash
cargo test -p cowd-memory l0_requires_user_or_system_write
cargo test -p cowd-memory l1_user_correction_supersedes_inference
cargo test -p cowd-memory l2_verified_code_fact_beats_stale_memory
cargo test -p cowd-memory l4_peer_private_scope_does_not_leak
cargo test -p cowd-memory legacy_files_are_not_auto_imported_on_startup
```

Acceptance:

- governance is enforced before memory becomes orientation.
- legacy local files are only imported after explicit user action.

### Phase 6: Distillation And Large-Scale Readiness

Goal:

- support large projects/docs without bloating active context.
- preserve full reachability while keeping runtime prepare cheap.

Work:

- trace-to-pattern distillation jobs.
- cold evidence retention.
- bounded path recall.
- benchmark corpus generation.
- topic cluster construction from evidence fingerprints, FTS terms, links, and optional embeddings.
- background job cancellation and degraded-result reporting.
- deduplication as archival/supersession, not destructive deletion by default.

TDD:

```bash
cargo test -p cowd-memory distillation_promotes_repeated_pattern
cargo test -p cowd-memory cold_trace_can_be_recalled_by_path
cargo test -p cowd-memory large_memory_prepare_context_stays_bounded -- --nocapture
cargo test -p cowd-memory distillation_failure_does_not_block_prepare
cargo test -p cowd-memory dedup_marks_superseded_without_losing_evidence
```

Acceptance:

- active orientation remains bounded while total evidence grows.
- cold knowledge remains reachable.
- background maintenance never blocks foreground agent/session execution.

### Phase 7: Product Surfaces

Goal:

- make memory understandable and controllable.
- expose memory as infrastructure, not as a decorative panel.

Work:

- `/memory why <query>`.
- `/memory states`.
- `/memory links`.
- TUI active orientation panel.
- WebUI recall explanation and conflict/supersession timeline.
- compact memory status line: layer budget, confidence, degraded state, conflicts.
- agent/session memory timeline with evidence links.

TDD/E2E:

```bash
cargo test -p cowd-cli memory_recall_explain_returns_real_packet
cargo test -p cowd-cli tui_memory_panel_shows_orientation_state
cargo test -p cowd-cli memory_status_reports_degraded_backend
```

Acceptance:

- user can inspect why memory was used, what evidence supports it, and what older knowledge was superseded.
- TUI/WebUI consume stable packet/state APIs rather than store internals.

### Parallel Work Matrix

Can run in parallel after Phase 1 starts:

- telemetry scaffolding.
- passive import notice UI.
- TUI/WebUI read-only memory status mock backed by packet fixtures.
- large-corpus fixture generator.

Must wait for Phase 2:

- conflict/supersession timelines.
- active vs historical state filters.
- archival/dedup semantics.

Must wait for Phase 3:

- path recall UI.
- symbol/entity/session/agent graph explanations.
- cold evidence path diagnostics.

Must wait for Phase 4:

- production TUI/WebUI memory explanation.
- API contract stabilization.
- browser E2E for memory inspection.

## Implementation Rules

1. Do not replace existing memory modules at once.
   Wrap, normalize, and migrate gradually.

2. Do not make embedding mandatory.
   External embedding config remains optional; FTS/link/path recall must work alone.

3. Do not treat graph/vector/wiki as independent product concepts.
   They are indexes/views over atoms.

4. Do not delete forgotten memory by default.
   Archive or supersede.

5. Do not allow memory errors to crash session/agent execution.
   Report degraded memory state.

6. Do not let sub-agents bypass governance.
   All agent memory writes go through MemoryKernel.

7. Do not allow self-evolution to mutate evidence.
   Evolution changes interpretations, links, states, salience, clusters, and summaries. Evidence remains immutable.

8. Do not make UI layer views independent state.
   L0-L4 surfaces are read projections over memory primitives.

9. Do not block foreground agent/session execution on background memory jobs.
   Distillation, embeddings, relinking, import, and health repair must be cancellable and degraded.

10. Do not activate memory that cannot explain itself.
    Orientation requires evidence, explicit authority, or a visible degraded/proposal state.

## First Concrete Work Package

Start with Phase 0 and Phase 1.

Files likely touched:

- `crates/memory/src/types.rs`
- `crates/memory/src/cognitive.rs`
- `crates/memory/src/orchestrator.rs`
- new `crates/memory/src/kernel.rs`
- `crates/memory/src/lib.rs`
- `crates/runtime/src/conversation.rs`
- `crates/runtime/src/agent.rs`
- `crates/runtime/src/joint_problem_solving.rs`
- `crates/cowd-cli/src/api_routes.rs`
- TUI memory panel integration later.

Minimal first API:

```rust
pub struct MemoryTurnContext {
    pub session_id: String,
    pub project_id: Option<String>,
    pub agent_id: String,
    pub team_id: Option<String>,
    pub task_id: Option<String>,
}

pub struct MemoryKernel {
    manager: Arc<CognitiveContextManager>,
}

pub struct MemoryHealth {
    pub orientation_pressure: f32,
    pub conflict_pressure: f32,
    pub stale_pressure: f32,
    pub evidence_coverage: f32,
    pub link_coverage: f32,
    pub background_lag_ms: Option<u64>,
    pub degraded: Vec<MemoryDegradation>,
}

pub enum MemoryDegradation {
    StoreUnavailable,
    FtsUnavailable,
    VectorUnavailable,
    LinkTraversalLimited,
    DistillationBacklog,
    ImportFailed,
    MalformedAtomSkipped,
}

impl MemoryKernel {
    pub async fn prepare(&self, ctx: &MemoryTurnContext, query: &str, messages: &[Message])
        -> MemoryKernelResult<PreparedContext>;

    pub async fn post_turn(&self, ctx: &MemoryTurnContext, messages: &mut Vec<Message>)
        -> MemoryKernelResult<()>;

    pub async fn health(&self, ctx: &MemoryTurnContext)
        -> MemoryKernelResult<MemoryHealth>;
}
```

First acceptance:

- no direct new runtime call to `CognitiveContextManager::prepare_context` outside kernel.
- session/agent context is bound before prepare/post-turn.
- degraded memory path is structured and non-fatal.

## Final Shape

The final memory system should feel simple:

```text
L0-L4 decide who may know, write, and override.
Trace/Pattern/Orientation decide how knowledge lives.
MemoryKernel decides how every session and agent uses it.
Links decide how cold knowledge becomes reachable.
State decides what is active, historical, superseded, or archived.
Recall decides the shortest reliable path to current valid orientation.
```

That is the pure architecture.

It uses the strengths already present in Cowd, but removes accidental complexity by giving every part one role.
