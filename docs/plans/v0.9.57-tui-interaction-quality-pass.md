# v0.9.57 TUI Interaction Quality Pass

## Goal

Make the TUI runtime model understandable during daily use by clearly showing that local control uses the daemon Unix socket while HTTP remains the projection/API surface.

## Scope

- Add a Gateway panel transport line: `control socket · projection http`.
- Rename the endpoint section to `HTTP Projection Endpoints`.
- Preserve existing Gateway panel density and key hints.
- Update render tests so the socket/http split remains visible.

## Validation

- `cargo test -p cowd-cli render_running_state --no-default-features`
- `cargo test -p cowd-cli render_shows_api_endpoints --no-default-features`
- `cargo fmt --check`
- `git diff --check`
